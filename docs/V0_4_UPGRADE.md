# v0.4 Upgrade

Adds sanitized precedent mining, evaluator traceability simulation, stronger bid-state provenance, reviewer-finding fixes, reproducible behavioral run metadata, and end-to-end fixtures. Repository CI validates the candidate using validators from the candidate checkout, so external review and platform security controls remain independent evidence rather than being implied by the local validator result.
